# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/hrms_db
spring.datasource.username=postgres
spring.datasource.password=admin123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080

# Auto-run data.sql on startup (works with external DBs like PostgreSQL)
spring.sql.init.mode=always
# Run data.sql AFTER Hibernate creates/updates the schema
spring.jpa.defer-datasource-initialization=true

# JWT (override in production; must be >= 32 characters for HS256)
jwt.secret=ChangeThisToAVeryLongSecretKeyAtLeast32CharsForHS256!!
