DB_URL=jdbc:postgresql://localhost:5432/hrms_db
DB_USERNAME=postgres
DB_PASSWORD=change-me
JWT_SECRET=replace-with-a-secret-of-at-least-32-characters
# Optional: override dev server port if 8080 is already taken.
# SERVER_PORT=8081
